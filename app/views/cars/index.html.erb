<h1>Carros</h1>

<% if current_user.admin? %>
    <p><%= link_to 'Registrar novo carro', new_car_path %></p>
<% end %>
<p><%= link_to 'Voltar', root_path %></p>

<% if @cars.empty? %>
    <p> Não existem carros cadastrados</p>
<% end %>

<% @cars.each do |car| %>
    <p><%= link_to "#{car.car_model.name}: #{car.license_plate}", car_path(car) %></p>
<% end %>


