<% @clients.each do |c| %>
    <h1><%= link_to c.name, client_path(c.id) %></h1>
<% end %>

<% if @clients.empty? %>
    <p> Não existem clientes cadastrados</p>
<% end %>

<p><%= link_to 'Registrar novo Cliente', new_client_path %></p>

<p><%= link_to "Voltar", root_path %></p>